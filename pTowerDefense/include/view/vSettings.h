#ifndef VSETTINGS_H
#define VSETTINGS_H


class vSettings
{
    public:
        vSettings();
        virtual ~vSettings();
        vSettings(const vSettings& other);
        vSettings& operator=(const vSettings& other);

    protected:

    private:
};

#endif // VSETTINGS_H
