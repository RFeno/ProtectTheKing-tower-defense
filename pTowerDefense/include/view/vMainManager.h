#ifndef VMAINMANAGER_H
#define VMAINMANAGER_H


class vMainManager
{
    public:
        vMainManager();
        virtual ~vMainManager();
        vMainManager(const vMainManager& other);
        vMainManager& operator=(const vMainManager& other);

    protected:

    private:
};

#endif // VMAINMANAGER_H
